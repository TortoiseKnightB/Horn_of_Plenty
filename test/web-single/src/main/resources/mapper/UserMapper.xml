<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knight.web.dao.UserMapper">

    <resultMap id="UserInfoMap" type="com.knight.web.model.entity.UserInfoDO">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="getUserInfo" resultMap="UserInfoMap">
        select *
        from userInfo
        where name = #{name}
    </select>

    <select id="getUserInfoList" resultMap="UserInfoMap">
        select *
        from userInfo
    </select>

    <insert id="insertUserInfoList">
        INSERT INTO userInfo (`name`, `password`)
        VALUES
        <foreach collection="userInfoDOList" item="userInfo" separator=",">
            (#{userInfo.name},#{userInfo.password})
        </foreach>
    </insert>

</mapper>


