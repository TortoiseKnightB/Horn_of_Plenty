spring:
  profiles:
    active: local

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # swagger3.0需要此路径匹配策略

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  config-location: classpath:mybatis-config.xml


logging:
  config: classpath:logback-spring.xml

